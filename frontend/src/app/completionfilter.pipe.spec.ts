import { CompletionfilterPipe } from './completionfilter.pipe';

describe('CompletionfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletionfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
